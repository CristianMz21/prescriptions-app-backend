/* Copyright (c) 2026. All rights reserved. */

// Real-time admin metrics SSE tick interval (milliseconds).
export const SSE_TICK_MS = 5000;

// Maximum length for free-text query parameters used in search filters.
export const SEARCH_QUERY_MAX_LENGTH = 200;

// Maximum length for the rendered doctor signature label.
export const SIGNATURE_TEXT_MAX_LENGTH = 120;

// Maximum length for the prescription consume audit reason.
export const CONSUME_REASON_MAX_LENGTH = 500;

// SMTP defaults (STARTTLS port; SSL/TLS-secure port).
export const SMTP_DEFAULT_PORT = 587;
export const SMTP_SECURE_PORT = 465;

// Password validation.
export const PASSWORD_MIN_LENGTH = 6;
